import { TestBed } from '@angular/core/testing';

import { CustomersWithTenantsService } from './customers-with-tenants.service';

describe('CustomersWithTenantsService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: CustomersWithTenantsService = TestBed.get(CustomersWithTenantsService);
  //   expect(service).toBeTruthy();
  // });
});
