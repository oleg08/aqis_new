import { TestBed } from '@angular/core/testing';

import { CheckPatternService } from './check-pattern.service';

describe('CheckPatternService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: CheckPatternService = TestBed.get(CheckPatternService);
  //   expect(service).toBeTruthy();
  // });
});
