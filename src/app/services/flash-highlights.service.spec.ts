import { TestBed } from '@angular/core/testing';

import { FlashHighlightsService } from './flash-highlights.service';

describe('FlashHighlightsService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: FlashHighlightsService = TestBed.get(FlashHighlightsService);
  //   expect(service).toBeTruthy();
  // });
});
